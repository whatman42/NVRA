# NVRA Oracle Free Tier environment (no secrets in this example).
# Copy to /etc/nvra/nvra.env and set permissions 0600.
# Do NOT commit real API keys or passwords.

NVRA_DATA_DIR=/var/lib/nvra
# Prefer token file over long-lived secrets in env:
# NVRA_SESSION_TOKEN=
