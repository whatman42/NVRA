# NVRA dependency constraints (production-oriented pins).
# Use: pip install -r requirements.txt -c constraints.txt
# Does not replace a full environment lock; pins security-sensitive and core packages.

# HTTP / networking
requests>=2.33.0,<3
urllib3>=2.7.0,<3
httpx>=0.25,<1

# Crypto / secrets
cryptography>=41,<47
keyring>=25,<26

# Config / core
numpy>=1.26,<2.0
PyYAML>=6.0,<7
psutil>=5.9,<8

# UI
PySide6>=6.6,<7

# Broker / market
ccxt>=4.2,<5

# ML stack (align with requirements.txt ranges)
scikit-learn>=1.3,<2
lightgbm>=4.0,<5
xgboost>=2.0,<4
catboost>=1.2,<2
torch>=2.3,<3.0
shap>=0.45,<1

# Test / packaging toolchain
pytest>=7.4,<9
pytest-asyncio>=0.21,<1
pytest-cov>=4.1,<7
pytest-mock>=3.11,<4
pyinstaller>=6.0,<7

# Optional ops
prometheus-client>=0.20,<1
google-auth>=2.29,<3
google-auth-oauthlib>=1.2,<2
google-api-python-client>=2.130,<3
