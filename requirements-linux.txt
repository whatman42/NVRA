# NVRA Linux / Oracle Free Tier headless runtime
# Product: NVRA | Developer: NUNG
# Does NOT install: MetaTrader5 (Windows-only), PyInstaller (build-only), PySide6 (GUI optional)
# Use: pip install -r requirements-linux.txt -c constraints.txt

numpy>=1.26,<2.0
PyYAML>=6.0
cryptography>=41
psutil>=5.9
requests>=2.33.0
httpx>=0.25
urllib3>=2.7.0
ccxt>=4.2
keyring>=25

# ML stack (same bounds as requirements.txt / constraints.txt)
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
torch>=2.3,<3.0
shap>=0.45

# Optional observability
prometheus-client>=0.20

# Test tools (CI / validation on Linux)
pytest>=7.4
pytest-asyncio>=0.21
pytest-cov>=4.1
pytest-mock>=3.11
